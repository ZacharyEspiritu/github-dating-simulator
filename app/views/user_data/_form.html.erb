<%= form_for(user_data) do |f| %>
  <% if user_data.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_data.errors.count, "error") %> prohibited this user_data from being saved:</h2>

      <ul>
      <% user_data.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %>
    <%= f.text_field :username %>
  </div>

  <div class="field">
    <%= f.label :data %>
    <%= f.text_area :data %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
